import { Meta, Canvas, Story } from '@storybook/addon-docs';
import { EsButton } from '@energysage/es-vue-base';

<Meta
    title="Design System/Atoms/EsButton"
    component={EsButton}
/>

export const Template = () => ({
    components: { EsButton },
    template: `<EsButton>Example</EsButton>`,
});

<Canvas>
    <Story
        name="Default"
        args={{
            default: 'My Button',
            variant: 'primary',
            size: 'md',
        }}>
        {Template.bind({})}
    </Story>
</Canvas>
