import { Meta, Canvas, Story } from '@storybook/addon-docs';
import {{ name }} from './{{ name }}.vue';

<Meta
    title="Design System/{{ size }}s/{{ name }}"
    component={ {{ name }} }
    argTypes=\{{}}
/>

export const Template = (args, { argTypes }) => ({
    props: Object.keys(argTypes),
    components: { {{ name }} },
    template: '<{{ name }} v-bind="$props" />',
});

# {{ name }}

<Canvas>
    <Story
        name="Default"
        args=\{{}}>
        {Template.bind({})}
    </Story>
</Canvas>
