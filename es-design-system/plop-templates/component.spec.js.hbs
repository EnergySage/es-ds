import { mount } from '@vue/test-utils';
import {{ name }} from '@/components/{{ name }}/{{ name }}.vue';
import jestVue from '@/jest/jest.vue.config';

describe('{{ name }}', () => {
    // Basic test; should exist for most components
    test('<{{ name }} />', async () => {
        const wrapper = mount({{ name }}, {
            ...jestVue,
        });
        const a11y = await axe(wrapper.element);

        expect(wrapper.vm).toBeTruthy();
        expect(wrapper.html()).toMatchSnapshot();
        expect(a11y).toHaveNoViolations();
    });

    // Using data-testid in conjuction with testid() is preferred
    test('check for <{{ name }} />', async () => {
        const wrapper = mount({{ name }}, {
            ...jestVue,
        });

        expect(wrapper.find(testid('example')).exists()).toBe(true);
    });
});
